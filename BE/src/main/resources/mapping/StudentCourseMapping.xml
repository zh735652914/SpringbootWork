<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.springbootWork.dao.mapper.StudentActivityMapper">
    <select id="getPage" resultType="StudentActivityItemVO">
        SELECT sc_id AS 'id', activity_name AS 'activityName', student_name AS 'studentName', class_name AS 'className',
        sc_daily_score AS 'dailyScore', sc_exam_score AS 'examScore', sc_score AS 'score'
        FROM rc_student_activity
        JOIN rc_activity on sc_activity_id = activity_id
        JOIN rc_student on sc_student_id = student_id
        JOIN rc_class on student_class_id = class_id
        <where>
            <if test="className != null and className != ''">
                AND class_name LIKE CONCAT('%',#{className},'%')
            </if>
            <if test="activityName != null and activityName != ''">
                AND activity_name LIKE CONCAT('%',#{activityName},'%')
            </if>
            <if test="studentName != null and studentName != ''">
                AND student_name LIKE CONCAT('%',#{studentName},'%')
            </if>
        </where>
    </select>

    <select id="count" resultType="Integer">
        SELECT COUNT(*)
        FROM rc_student_activity
        JOIN rc_activity on sc_activity_id = activity_id
        JOIN rc_student on sc_student_id = student_id
        JOIN rc_class on student_class_id = class_id
        <where>
            <if test="className != null and className != ''">
                AND class_name LIKE CONCAT('%',#{className},'%')
            </if>
            <if test="activityName != null and activityName != ''">
                AND activity_name LIKE CONCAT('%',#{activityName},'%')
            </if>
            <if test="studentName != null and studentName != ''">
                AND student_name LIKE CONCAT('%',#{studentName},'%')
            </if>
        </where>
    </select>

    <select id="listStudentActivitySelected" resultType="StudentActivitySelectedItemVO">
        SELECT sc_id          AS 'studentActivityId',
               activity_name    AS 'activityName',
               teacher_name   AS 'teacherName',
               activity_credit  AS 'credit',
               sc_daily_score AS 'dailyScore',
               sc_exam_score  AS 'examScore',
               sc_score       AS 'score'
        FROM rc_student_activity
                 JOIN rc_student on sc_student_id = student_id
                 JOIN rc_activity on sc_activity_id = activity_id
                 JOIN rc_teacher on activity_teacher_id = teacher_id
        WHERE student_id = #{studentId}
    </select>

    <select id="listStudentExam" resultType="StudentExamItemVO">
        SELECT sc_id                AS 'studentActivityId',
               activity_name          AS 'activityName',
               teacher_name         AS 'teacherName',
               activity_exam_date        'examDate',
               activity_exam_location AS 'examLocation'
        FROM rc_student_activity
                 JOIN rc_activity on sc_activity_id = activity_id
                 JOIN rc_student on sc_student_id = student_id
                 JOIN rc_teacher on activity_teacher_id = teacher_id
        WHERE student_id = #{studentId}
          AND activity_exam_date IS NOT NULL
          AND activity_exam_location IS NOT NULL
          AND sc_exam_score IS NULL
    </select>

    <select id="countStudentActivitySelectedByTimePart" resultType="Integer">
        SELECT COUNT(*)
        FROM rc_student_activity
                 JOIN rc_student on sc_student_id = student_id
                 JOIN rc_activity on sc_activity_id = activity_id
        WHERE student_id = #{studentId}
          AND activity_time LIKE CONCAT(#{timePart}, '-%')
    </select>

    <select id="listStudentTimetable" resultType="TimetableItemVO">
        SELECT activity_time     AS 'time',
               activity_name     AS 'activityName',
               teacher_name    AS 'teacherName',
               activity_location AS 'location'
        FROM rc_student_activity
                 JOIN rc_activity on sc_activity_id = activity_id
                 JOIN rc_teacher on activity_teacher_id = teacher_id
        WHERE sc_student_id = #{studentId}
    </select>

    <select id="getTeacherGradePage" resultType="TeacherGradeItemVO">
        SELECT sc_id AS 'studentActivityId', activity_name AS 'activityName',
        student_name AS 'studentName', sc_daily_score AS 'dailyScore',
        sc_exam_score AS 'examScore', sc_score AS 'score'
        FROM rc_student_activity
        JOIN rc_activity on sc_activity_id = activity_id
        JOIN rc_student on sc_student_id = student_id
        JOIN rc_teacher on activity_teacher_id = teacher_id
        <where>
            AND teacher_id = #{teacherId}
            <if test="activityName != null and activityName != ''">
                AND activity_name LIKE CONCAT('%',#{activityName},'%')
            </if>
            <if test="studentName != null and studentName != ''">
                AND student_name LIKE CONCAT('%',#{studentName},'%')
            </if>
        </where>
    </select>

    <select id="countTeacherGrade" resultType="Integer">
        SELECT COUNT(*)
        FROM rc_student_activity
        JOIN rc_activity on sc_activity_id = activity_id
        JOIN rc_student on sc_student_id = student_id
        JOIN rc_teacher on activity_teacher_id = teacher_id
        <where>
            AND teacher_id = #{teacherId}
            <if test="activityName != null and activityName != ''">
                AND activity_name LIKE CONCAT('%',#{activityName},'%')
            </if>
            <if test="studentName != null and studentName != ''">
                AND student_name LIKE CONCAT('%',#{studentName},'%')
            </if>
        </where>
    </select>
</mapper>