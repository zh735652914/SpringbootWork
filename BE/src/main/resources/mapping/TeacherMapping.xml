<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.springbootWork.dao.mapper.TeacherMapper">
    <select id="getPage" resultType="TeacherItemVO">
        SELECT teacher_id AS 'id', teacher_number AS 'number', teacher_name AS 'name'
        FROM rc_teacher
        <where>
            <if test="name != null and name != ''">
                AND teacher_name LIKE CONCAT('%',#{name},'%')
            </if>
        </where>
    </select>

    <select id="count" resultType="Integer">
        SELECT COUNT(*)
        FROM rc_teacher
        <where>
            <if test="name != null and name != ''">
                AND teacher_name LIKE CONCAT('%',#{name},'%')
            </if>
        </where>
    </select>

    <select id="listTeacherTimetable" resultType="TimetableItemVO">
        SELECT activity_time     AS 'time',
               activity_name     AS 'activityName',
               teacher_name    AS 'teacherName',
               activity_location AS 'location'
        FROM rc_teacher
                 JOIN rc_activity on teacher_id = activity_teacher_id
        WHERE teacher_id = #{teacherId}
    </select>

    <select id="listTeacherActivity" resultType="TeacherActivityItemVO">
        SELECT activity_id             AS 'id',
               activity_name           AS 'name',
               activity_time           AS 'time',
               activity_location       AS 'location',
               activity_selected_count AS 'selectedCount',
        FROM rc_teacher
                 JOIN rc_activity on teacher_id = activity_teacher_id
        WHERE teacher_id = #{teacherId}
    </select>
</mapper>