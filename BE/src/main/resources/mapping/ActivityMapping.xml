<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.springbootWork.dao.mapper.ActivityMapper">
    <select id="getPage" resultType="ActivityItemBO">
        SELECT activity_id AS 'id', activity_name AS 'name', teacher_name AS 'teacherName',
        activity_time AS 'time', activity_selected_count AS 'selectedCount', activity_max_size AS 'maxSize',
        activity_describe AS 'activityDescribe', activity_location AS 'activityLocation'
        FROM rc_activity
        JOIN rc_teacher on activity_teacher_id = teacher_id
        <where>
            <if test="teacherName != null and teacherName != ''">
                AND teacher_name LIKE CONCAT('%',#{teacherName},'%')
            </if>
            <if test="name != null and name != ''">
                AND activity_name LIKE CONCAT('%',#{name},'%')
            </if>
        </where>
    </select>

    <select id="count" resultType="Integer">
        SELECT COUNT(*)
        FROM rc_activity
        JOIN rc_teacher on activity_teacher_id = teacher_id
        <where>
            <if test="teacherName != null and teacherName != ''">
                AND teacher_name LIKE CONCAT('%',#{teacherName},'%')
            </if>
            <if test="name != null and name != ''">
                AND activity_name LIKE CONCAT('%',#{name},'%')
            </if>
        </where>
    </select>

    <select id="getConsumerCanSelectPage" resultType="ConsumerActivitySelectItemBO">
        SELECT DISTINCT activity_id AS 'activityId', activity_name AS 'activityName', teacher_name AS 'teacherName',
        activity_time AS 'time', activity_describe AS 'activityDescribe', activity_location AS activityLocation,
        activity_selected_count AS 'selectedCount', activity_max_size AS 'maxSize'
        FROM rc_activity
        JOIN rc_teacher on activity_teacher_id = teacher_id
        WHERE #{consumerId} = #{consumerId}
        AND  activity_id NOT IN (
        SELECT sc_activity_id
        FROM rc_consumer_activity
        WHERE sc_consumer_id = #{consumerId}
        )
        <if test="activityName != null and activityName != ''">
            AND activity_name LIKE CONCAT('%',#{activityName},'%')
        </if>
        <if test="teacherName != null and teacherName != ''">
            AND teacher_name LIKE CONCAT('%',#{teacherName},'%')
        </if>
        AND activity_selected_count &lt; activity_max_size
    </select>

    <select id="countConsumerCanSelect" resultType="Integer">
        SELECT COUNT(*) FROM ( SELECT DISTINCT activity_id
        FROM rc_activity
        JOIN rc_teacher on activity_teacher_id = teacher_id
        WHERE #{consumerId} = #{consumerId}
        AND activity_id NOT IN (
        SELECT sc_activity_id
        FROM rc_consumer_activity
        WHERE sc_consumer_id = #{consumerId}
        )
        <if test="activityName != null and activityName != ''">
            AND activity_name LIKE CONCAT('%',#{activityName},'%')
        </if>
        <if test="teacherName != null and teacherName != ''">
            AND teacher_name LIKE CONCAT('%',#{teacherName},'%')
        </if>
        AND activity_selected_count &lt; activity_max_size) cnt
    </select>

    <select id="countVisitorCanSelect" resultType="Integer">
        SELECT COUNT(*) FROM ( SELECT DISTINCT activity_id
        FROM rc_activity
        JOIN rc_teacher on activity_teacher_id = teacher_id
        <if test="activityName != null and activityName != ''">
            AND activity_name LIKE CONCAT('%',#{activityName},'%')
        </if>
        <if test="teacherName != null and teacherName != ''">
            AND teacher_name LIKE CONCAT('%',#{teacherName},'%')
        </if>
        AND activity_selected_count &lt; activity_max_size) cnt
    </select>

    <select id="getVisitorCanSelectPage" resultType="ConsumerActivitySelectItemBO">
        SELECT DISTINCT activity_id AS 'activityId', activity_name AS 'activityName', teacher_name AS 'teacherName',
        activity_time AS 'time', activity_describe AS 'activityDescribe', activity_location AS activityLocation,
        activity_selected_count AS 'selectedCount', activity_max_size AS 'maxSize'
        FROM rc_activity
        JOIN rc_teacher on activity_teacher_id = teacher_id
        <if test="activityName != null and activityName != ''">
            AND activity_name LIKE CONCAT('%',#{activityName},'%')
        </if>
        <if test="teacherName != null and teacherName != ''">
            AND teacher_name LIKE CONCAT('%',#{teacherName},'%')
        </if>
        AND activity_selected_count &lt; activity_max_size
    </select>
</mapper>