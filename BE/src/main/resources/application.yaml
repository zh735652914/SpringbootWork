server:
  port: 8085

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/home_work?serverTimezone=CTT&useUnicode=true&characterEncoding=utf-8&allowMultiQueries=true
    username: root
    password: 735652914
    schema:
      - classpath:database/rainng_course.sql
#      - classpath:database/homeWork.sql
    initialization-mode: ALWAYS
    platform: mysql
  redis:
    host: localhost
    port: 6379
  session:
    store-type: redis
    timeout: 86400
  jackson:
    time-zone: Asia/Shanghai
  data:
    mongodb:
      uri: mongodb://localhost:27017/homeWork

mybatis-plus:
  mapper-locations: classpath:mapping/*.xml
  type-aliases-package: com.springbootWork.model